apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: toks4-compute-env-secrets-reader
  namespace: cogni-preview
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    resourceNames:
      - toks4-compute-env-secrets
    verbs:
      - get
