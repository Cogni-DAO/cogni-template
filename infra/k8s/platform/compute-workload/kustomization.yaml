# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2026 Cogni-DAO
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Compatibility redirect for local consumers. Deploy branches materialize base/**,
  # so the canonical Argo child target lives there.
  - ../../base/compute-workload-platform
